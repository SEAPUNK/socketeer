ServerClient state flags
===

Development reference: Used to explain which variable is which, and what it's used for.

* `_isReady`
* `_handshakeOver`
* `_closeIsPause`
* `_sessionToken`
* `_handshakeFinished`
* `_handshakePromise`
* `_handshakeResolve`
* `_handshakeReject`